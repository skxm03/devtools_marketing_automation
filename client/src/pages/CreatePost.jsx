import React from 'react';

const CreatePost = () => {
	return (
		<div>
			{/* Create Post Page - Form to create new post with template selection, image upload, scheduling */}
		</div>
	);
};

export default CreatePost;
