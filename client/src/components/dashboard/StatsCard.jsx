import React from 'react';

const StatsCard = ({ title, value, icon, color }) => {
	return (
		<div>
			{/* Stats Card - Display stat like "Total Posts: 24", "Scheduled: 5", etc. */}
		</div>
	);
};

export default StatsCard;
