import React from 'react';

const UpcomingPosts = ({ posts }) => {
	return (
		<div>
			{/* Upcoming Posts - List of next scheduled posts on dashboard */}
		</div>
	);
};

export default UpcomingPosts;
