import React from 'react';

const TemplateSelector = ({ onSelectTemplate }) => {
	return (
		<div>
			{/* Template Selector - Modal/Dropdown to select a template when creating post */}
		</div>
	);
};

export default TemplateSelector;
