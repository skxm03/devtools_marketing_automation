import React from 'react';

const TemplateCard = ({ template, onSelect }) => {
	return (
		<div>
			{/* Template Card - Display single template with preview and select button */}
		</div>
	);
};

export default TemplateCard;
