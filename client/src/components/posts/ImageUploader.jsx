import React from 'react';

const ImageUploader = ({ onImageUpload, currentImage }) => {
	return (
		<div>
			{/* Image Uploader - Drag & drop or click to upload image, preview */}
		</div>
	);
};

export default ImageUploader;
