import React from 'react';

const PostForm = ({ post, onSubmit, isEdit = false }) => {
	return (
		<form>
			{/* Post Form - Form for creating/editing posts (event name, caption, image, schedule) */}
		</form>
	);
};

export default PostForm;
